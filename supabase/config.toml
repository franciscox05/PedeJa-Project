# Public checkout endpoint for guest users.
[functions.create-order]
verify_jwt = false

# Signed upload URLs for store/menu images -- public because guest partner signups
# upload images before any account/session exists (ownership is validated inside
# the function for the authenticated edit paths).
[functions.request-image-upload]
verify_jwt = false
